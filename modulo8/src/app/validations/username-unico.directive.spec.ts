import { UsernameUnicoDirective } from './username-unico.directive';

describe('UsernameUnicoDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameUnicoDirective();
    expect(directive).toBeTruthy();
  });
});
